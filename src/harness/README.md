V0：
  workspace + tools + tests + reward

V1：
  tool policy + command allowlist + diff + eval runner

V2：
  AgentAction / Observation + action loop

V3：
  JSON action planner

V4：
  success/failure trajectories + DPO pair builder

V5：
  LLM planner interface + prompt builder + parse/retry